# Roadmap

A running list of future work for the Frontier Firm Agent Starter Kits site.
Items are grouped by priority. Move things up as they become relevant.

## High priority

- _(none yet)_

## Medium priority

- **Grow the kit catalog.** Add the next agent starter kits (target ~6 in the near term, scaling toward 35). Each new kit = publish a GitHub Release with a stable `<kit>.zip` asset name + add a card in `index.html`.

## Low priority

- **Page-view analytics.** GitHub Pages has no native visitor tracking. Add a cookieless analytics snippet (e.g. Cloudflare Web Analytics) to `index.html` to measure page views. Pending Microsoft privacy/telemetry approval before adding any third-party tracker. (Release-asset download counts are already tracked automatically by GitHub.)

## Done

- Published the site via GitHub Pages at https://microsoft.github.io/frontier-agent-starter-kits/.
- Centered the kits grid (max 3 columns, centered remainder row).
- Wired the Job Description Agent card to a one-click latest-release download.
